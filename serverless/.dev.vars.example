# Local testing only. Copy this file to `.dev.vars` and fill in your key, then run:
#     wrangler dev
# NEVER commit the real `.dev.vars` (it holds your live key). This .example file is safe.

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
# TURNSTILE_SECRET=0x_optional_turnstile_secret_key
